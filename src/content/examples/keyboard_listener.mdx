---
title: キーボードリスナー
id: 26.1
date: 2023-07-15
---

import ExampleCode from '../../components/ExampleCode.astro';
import ExampleLink from '../../components/ExampleLink.astro';
import DocLink from '../../components/DocLink.astro';

## KeyboardListenerComponent

以前 <ExampleLink slug="keyboard_events" />では`KeybaordEvents` mixin を使ってキーボード入力を自分で処理しましたが、
単純にキーボード入力を処理するだけなら`KeyboardListenerComponent`を使うと簡単にできます。

<DocLink document="https://docs.flame-engine.org/latest/flame/inputs/keyboard_input.html#receive-keyboard-events-in-a-component-level" />
<DocLink reference="https://pub.dev/documentation/flame/latest/components/KeyboardListenerComponent-class.html" />

## 動かす

ゲームクラスに `HasKeyboardHandlerComponents` mixin を指定します。

<ExampleCode file="keyboard_listener/game.dart" label="keyboard_listener1" />

次に`KeyboardListenerComponent`を使ってキーボード入力を処理します。
行数は多いですが、KeyDown/KeyUp 時の処理を単純に指定するだけなので簡単です。

<ExampleCode file="keyboard_listener/game.dart" label="keyboard_listener2" />

これで<ExampleLink slug="keyboard_events" />と同じようにキーボード入力を処理できるようになりました。
