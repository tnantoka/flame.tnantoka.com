---
import * as fs from 'node:fs';
import { Code } from 'astro/components';

export interface Props {
  dir?: string;
  lang?: string;
  file: string;
  label: string;
}

const {
  file,
  label,
  dir = 'examples/lib/examples',
  lang = 'dart',
} = Astro.props;
const path = `flutter/${dir}/${file}`;
let code = fs.readFileSync(path, 'utf8');
if (label) {
  code = code
    .replace(new RegExp(`[\\s\\S]*<${label}>.*$`, 'm'), '')
    .replace(new RegExp(`^.*</${label}>[\\s\\S]*`, 'm'), '')
    .replace(/^[^\n]*\n/, '')
    .replace(/[\s]*\n$/, '\n');
}
---

<div class="bg-secondary-subtle px-3 py-1">
  <a
    href={`https://github.com/tnantoka/flame.tnantoka.com/tree/main/${path}`}
    class="text-decoration-none text-reset icon-link"
    target="_blank"
    rel="noopener"
  >
    {file}
    <i class="bi-box-arrow-up-right"></i>
  </a>
</div>
<Code code={code} lang={lang} />
